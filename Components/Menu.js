import Link from  'next/link';

const Menu = () => {
    return(
        <div>
            <Link href={'/sample-page'}>Sample page</Link>
            <span onClick={} >Authorization</span>
        </div>
    )
}

export default Menu